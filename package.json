{
  "name": "gnome-widget-center-store",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply gwc-store --local",
    "migrate:remote": "wrangler d1 migrations apply gwc-store --remote",
    "test": "node --experimental-sqlite test/integration.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
